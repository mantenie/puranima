{
  "name": "puranima",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Puranima — Gewissenserforschung PWA",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/build.js --watch",
    "preview": "npx serve dist -l 3000",
    "audit-pdf": "node scripts/generate-audit-pdf.js"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "esbuild": "^0.25.0",
    "pdfmake": "^0.2.18",
    "sharp": "^0.34.5",
    "tailwindcss": "^4.0.0"
  }
}
