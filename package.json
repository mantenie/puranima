{
  "name": "puranima",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Puranima — Gewissenserforschung PWA",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/build.js --watch",
    "preview": "npx serve dist -l 3000"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "esbuild": "^0.25.0",
    "sharp": "^0.34.5",
    "tailwindcss": "^4.0.0"
  }
}
